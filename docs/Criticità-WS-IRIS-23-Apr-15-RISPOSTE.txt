1) Non avere informazioni sull'owner del documento significa che non abbiamo al momento un equivalente per i campi UGOV:

ID_UTENTE_PROPRIETARIO
ID_ANAG_PROPRIETARIO
DS_PROPRIETARIO

RISPOSTA: L'owner verrà aggiunto nella versione in rilascio per lunedì. Ovviamente è tutto riferito a IRIS come id interno e non a UGOV

2) Per quanto riguarda gli autori riconosciuti invece, questi non erano in UGOV presenti in campi specifici, ma si ottenevano con una chiamata separata a 

getAutoriProdotto()

passando come argomento l'id_UGOV del prodotto di cui si volevano gli autori. Adesso in IRIS invece sono embedded nel documento, ma vengono forniti degli id interni di IRIS, che senza ulteriori servizi non ci consentono di reperire le informazioni di carriera relative. Queste invece in UGOV si reperivano tramite una chiamata alla funzione

RISPOSTA: Gli autori sono presenti come metadati del prodotto. Attraverso il servizio di anagrafica si recuperano tutte le informazioni a partire dell'id interno di IRIS.

getAnagrafica()

passando l'id_ab come argomento.

RISPOSTA: è in corso di realizzazione

3) Nel caso delle riviste non abbiamo corrispondenze per i campi UGOV:

TITOLO_UFFICIALE
ISSN

In IRIS ci viene fornito, in corrispondenza della chiave dc.authority.ancejournal un id interno, che non ci consente di ottenere altre informazioni senza un ulteriore servizio da interrogare.

RISPOSTA: esiste già un servizio REST di recupero della rivista che può essere utile per l'inserimento dato l'ance o l'issn. Sto aggiungendo anche l'interrogazione contraria. Versione di lunedi.

4) Le query per data di ultima modifica venivano effettuate in UGOV tramite la chiamata alla funzione:

getProdottiModificati()

Non abbiamo al momento ancora a disposizione una funzionalita' equivalente nei servizi rest di IRIS.

RISPOSTA: in realizzazione.Prensente nella versione di lunedì.

5) Lo stato di un prodotto (provvisorio/definitivo) veniva acceduto in UGOV tramite il campo:

DS_STATO

In IRIS sono presenti i due campi di controllo "workFlowValidationStatus" e "workFlowValidationRule", che immaginiamo svolgano una funzione analoga, ma per i quali al momento non abbiamo documentazione.

RISPOSTA: E' necessario fornire documentazione assieme ad altre cose. Appena finito sviluppo di lunedì aggiungo informazioni. Sarebbe richiesto intervento di chi conosce il prodotto in modo più approfondito.

6) Al posto del campo UGOV:

LINGUE_ABBREV

stiamo usando provvisoriamente il valore associato alla chiave dc.language.iso, ma in assenza di documentazione non siamo certi della compatibilita' tra i due formati.

RISPOSTA: Confermo. Aggiungere a documentazione.